# How-to guides

<!-- TODO: An index of task-oriented recipes, one page per task. The tasks this
template actually gets asked about: adding a function to `-core` and exposing
it through `-ffi`, returning a typed error across the FFI boundary, adding a
dependency to the right crate, and building a wheel for release.
Task-oriented — assumes the reader knows what they want and needs the steps.
Fenced `python` blocks here run in isolation; each must stand alone without
`continuation`. -->
