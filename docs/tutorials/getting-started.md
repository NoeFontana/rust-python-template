# Getting started

<!-- TODO: A ten-minute learning path from a clean clone to a first working
result: `just bootstrap`, import the wrapper, call `add()`, then change the
Rust implementation in `-core` and see the change surface in Python after
`just develop`. That round trip is the thing this template exists to teach.
Learning-oriented — a guaranteed-to-succeed sequence, not a menu of options.
Every fenced `python` block after the first uses the `continuation` fence
option so state carries across the page. -->
